# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - release*
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DUNKSTORMEN_DK_CCDD6 }}'
          channelId: live
          projectId: dunkstormen-dk-ccdd6
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
  webhook_pass:
    runs-on: ubuntu-latest
    needs: build_and_deploy
    steps:
      - name: Invoke workflow passed webhook
        uses: distributhor/workflow-webhook@v1
        env:
          webhook_url: ${{ secrets.WEBHOOK_PASS_URL }}
          webhook_secret: ${{ secrets.WEBHOOK_PASS_URL }}
